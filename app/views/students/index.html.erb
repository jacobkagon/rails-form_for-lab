<h1>Student List</h1>
<ul>
    <%@students.each do |student|%>
<li><%=link_to "#{student.first_name}  #{student.last_name}", student_path(student.id)%></li>
<%end%>
</ul>
<p><%=link_to "Add a Student", students_new_path%></p>